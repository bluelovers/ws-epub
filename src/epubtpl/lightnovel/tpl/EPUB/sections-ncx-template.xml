<navPoint id="{{name}}">
    <navLabel>
        <text>{{content.title}}</text>
    </navLabel>
    {{#if needPage}}
    <content src="{{name}}.html" title="{{content.title}}" />
    {{/if}}
    {{#each subSections}} {{> sectionsNCXTemplate}} {{/each}}
</navPoint>
